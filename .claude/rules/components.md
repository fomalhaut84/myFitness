# 컴포넌트 규칙

이 규칙은 UI 컴포넌트 파일에 적용.

- 함수형 컴포넌트 + hooks만 사용. class 컴포넌트 금지.
- default export 사용.
- UI 텍스트는 한국어. props/변수명은 영어.
- 수치 표시: 거리 km(소수점 2자리), 심박수 bpm(정수), 칼로리 kcal(정수), 체중 kg(소수점 1자리).
- 다크 테마 기본, 모바일 반응형.
