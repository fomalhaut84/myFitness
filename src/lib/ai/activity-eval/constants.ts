// #440: route 파일은 핸들러 외 export 를 허용하지 않아 (Next route 필드 검사) 상수는 여기.
/** 어드바이저 세션 채널 — `/ai` 채팅 (web) 과 분리, 호출마다 리셋 */
export const ACTIVITY_EVAL_CHANNEL = "activity-eval";
/** `AIAdvice.category` */
export const ACTIVITY_EVAL_CATEGORY = "activity_eval";
