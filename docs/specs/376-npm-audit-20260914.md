# [chore] npm audit 취약점 4건 해소 — Security Audit 2026-09-14

- **작성일**: 2026-09-17
- **타입**: chore (의존성)
- **이슈**: #376
- **범위**: `package.json` (직접 의존성 patch 상향 1쌍 + overrides 3건) · `package-lock.json`. 코드 변경 없음.

## 1. 배경

`Security Audit` 워크플로우(2026-09-14 main)가 4건을 잡아 #376 을 자동 생성했다. CLAUDE.md 「의존성 취약점 대응」 절차대로 `npm audit --json` 으로 범위·유입 경로·패치 버전을 확인했다.

| 패키지 | 심각도 | 취약 범위 | 설치 | 유입 | advisory |
|---|---|---|---|---|---|
| `next` | **critical** | 16.0.0 – 16.3.2 | 16.3.0 (직접) | — | GHSA-p293-qw3h-jr36 (Image Optimizer RCE 등) |
| `sharp` | high | < 0.35.4 | 0.35.3 | `next` 전이 | GHSA-rgj7-g3m4-5g8c (libheif) |
| `js-yaml` | high | 4.0.0 – 4.3.1 | 4.3.1 (override) | `@eslint/eslintrc` | GHSA-2883-xcg3-v3hh |
| `hono` | moderate | ≤ 4.13.4 | 4.13.0 (override) | `@modelcontextprotocol/sdk` | GHSA-gqvv-2mrq-wpjv |

## 2. 수정

| 패키지 | 현재 | 변경 | 종류 | 근거 |
|---|---|---|---|---|
| `next` | `^16.2.6` (16.3.0) | `^16.3.5` | **patch** (같은 16.3) | 취약 범위 상한 16.3.2 → 최소 16.3.3, 최신 patch 16.3.5 |
| `eslint-config-next` | `^16.2.6` | `^16.3.5` | patch | next 와 버전 쌍 유지 |
| overrides `sharp` | 신규 | `^0.35.4` | patch | next 전이. 패치본 0.35.4 |
| overrides `js-yaml` | `^4.2.0` | `^4.3.2` | patch | 5.x 는 major — eslintrc 가 4.x 요구. 같은 major 패치본 |
| overrides `hono` | `^4.12.21` | `^4.13.5` | minor (4.x 안) | 취약 상한 4.13.4 → 4.13.5+ (설치 4.13.8) |

**직접 의존성 major 업그레이드 없음.** lockfile 은 버전 상향만 — `node_modules/` 엔트리 추가·삭제 0건. `npm audit`: **0건**.

## 3. 검증

- 4종: `lint / typecheck / test / build` — `next build` 가 16.3.5 로 통과해야 한다 (핵심 런타임 검증)
- MCP `tools/list` 22개 + 집계 도구 호출 (hono 는 MCP SDK 의 HTTP 전송 의존성)
- `gh workflow run "Security Audit" --ref fix/376-1` → success
- 배포 후: 웹 UI 이미지(활동 사진·식단 사진) 렌더 — `sharp`/Image Optimizer 경로

## 4. 제외

- `js-yaml` 5.x, `hono` 5.x 등 major 는 별도 판단 (현재 필요 없음)
